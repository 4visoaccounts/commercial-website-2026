{# ========================================================================== #}
{# Inheritance #}
{# ========================================================================== #}
{% extends "_layouts/base.twig" %}

{# ========================================================================== #}
{# Content #}
{# ========================================================================== #}
{% block content %}
    {% cache globally using key "page-#{entry.slug}" if not craft.app.config.general.devMode  %}
        {# Hero #}
        {% include "/hero/_index.twig" %}

        {# Body #}
        {% include "/body/_index.twig" %}
    {% endcache %}
{% endblock %}