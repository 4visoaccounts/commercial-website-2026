{# ========================================================================== #}
{# :: Variables #}
{# ========================================================================== #}
{% set hideMenu = hideMenu is defined ? hideMenu : false %}

{# ========================================================================== #}
{# Header Index #}
{# ========================================================================== #}
